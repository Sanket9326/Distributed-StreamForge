# StreamForge Web

Angular 22 client for the StreamForge upload slice. It sends one validated video
to the same-origin `/api/uploads` endpoint and reports progress, cancellation,
errors, and the returned upload receipt.

Use the repository [development setup](../../docs/development/setup.md) for the
supported Node version and verified commands.
